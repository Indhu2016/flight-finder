# Email (SMTP). For Gmail, you need an App Password (not your normal password).
# SECURITY: Never commit the actual .env file. Use strong, unique passwords.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_NAME=Smart Travel Optimizer

# Providers (optional; leave empty to use only mock provider)
# SECURITY: Keep these API keys secret. Rotate them regularly.
AMADEUS_API_KEY=
AMADEUS_API_SECRET=
OMIO_API_KEY=

# Security Settings (optional)
# RATE_LIMIT_REQUESTS_PER_MINUTE=60
# MAX_EMAIL_SIZE_KB=1024
